# Eval: csharp — 2026-07-30 (run 2)

Fixture: `harness/fixtures/dotnet/` · Target: `OrderService.cs` + `OrdersController.cs` · Cold-start generation.
**Purpose of run 2:** verify the post-Java-removal rules and the new Arrange-Act-Assert (AAA) convention for C#.

| Dimension | Result | Gate | Verdict |
|---|---|---|---|
| Compiles | `dotnet build` exit 0, 0 warnings (first attempt) | HARD | PASS |
| Passes | `dotnet test` exit 0 — 13/13 passed (first run, zero fix attempts) | HARD | PASS |
| Case recall | 13/13 golden branches covered = 1.00 | ≥0.90 | PASS |
| Case precision | 0 EXCLUDE violations | 0 | PASS |
| Forbidden patterns | 0 hits (all greps clean; 0 `Arg.Any` inside `Received()`) | 0 | PASS |
| Conventions | 8/8 applicable = 1.00 — **AAA comments confirmed**: 22 `// Arrange`/`// Act`/`// Assert` markers, 0 Given-When-Then in C# code; `// Act & Assert` for exception tests; `actual`/`expected` prefixes | ≥0.90 | PASS |

**Overall: PASS**

## Improvements over run 1

- AAA convention followed (run 1 predated the rule change and used GWT).
- Fixture `OrderService.sln` untouched this run — the agent built the test csproj directly. Zero tracked-file drift during generation.
- `<RollForward>LatestMajor</RollForward>` included up front (run 1 discovered it by trial).

## Misses

None. Recall exact: both private `CalculateDiscount` branches via `CreateOrder` inputs (boundary 100m pinned), all 4 controller status codes separate, correctly skipped `[ApiController]` auto-400 per `controller-test-rules.md`.

## Notes

- Test artifacts removed and fixture verified clean after scoring, per rubric.
