# Eval: csharp — 2026-07-30

Fixture: `harness/fixtures/dotnet/` · Target: `OrderService.cs` + `OrdersController.cs` · Cold-start generation (golden list and rubric withheld from the generating agent).

| Dimension | Result | Gate | Verdict |
|---|---|---|---|
| Compiles | `dotnet build` exit 0, 0 warnings (first attempt) | HARD | PASS |
| Passes | `dotnet test` exit 0 — 14/14 passed | HARD | PASS |
| Case recall | 13/13 golden branches covered = 1.00 | ≥0.90 | PASS |
| Case precision | 0 EXCLUDE violations | 0 | PASS |
| Forbidden patterns | 0 hits (WebApplicationFactory, TestServer, Testcontainers, JsonSerializer.Serialize, JsonConvert, ServiceCollection/BuildServiceProvider, Arg.Any-in-Received: all 0) | 0 | PASS |
| Conventions | 8/8 applicable checklist items = 1.00 | ≥0.90 | PASS |

**Overall: PASS**

## Recall detail

All 13 golden cases matched, including both private `CalculateDiscount` branches driven through `CreateOrder` inputs (boundary total=100m pinned) and all four controller status codes as separate tests (200/404/201/400). One extra generated case beyond the golden list — `CreateOrder_ValidRequest_ReturnsOrderSavedByRepository` (return-value passthrough) — judged a distinct observable behavior, not an EXCLUDE violation.

## Misses

None.

## Notes

- Generated with NSubstitute `Arg.Do<Order>` capture + field assertions (no `Arg.Any` in verification), direct controller instantiation, PascalCase `Method_State_Outcome` naming, `actual`/`expected` prefixes.
- Correctly declined to unit-test `[ApiController]` automatic model validation (pipeline concern), per `controller-test-rules.md`.
- **Convention change mid-run:** this run was generated under the original Given-When-Then comment rules. C# rules were switched to Arrange-Act-Assert (AAA) comments immediately after generation started (maintainer directive). GWT comments accepted for THIS run; the next csharp run must produce `// Arrange` / `// Act` / `// Assert`.
- Environment: machine has .NET 9/10 runtimes only; generating agent added `<RollForward>LatestMajor</RollForward>` to the generated **test** csproj (fixture untouched, still net8.0). Legitimate; future runs on this machine will need the same.
- Fixture integrity: nothing under `src/` modified. `OrderService.sln` gained the test project via `dotnet sln add` (required to build); reset after scoring.
- Test artifacts removed and fixture reset to HEAD after scoring, per rubric.
