# Eval: csharp — 2026-08-13

Fixture: `harness/fixtures/dotnet/` · Target: `OrderService.cs` + `OrdersController.cs` · Cold-start generation (golden list and rubric withheld from the generating agent). Run against the enterprise-hardened skill (Step 0 target validation, Boundaries, dependency guardrail, pre-existing-test protection, scoped verification).

| Dimension | Result | Gate | Verdict |
|---|---|---|---|
| Compiles | `dotnet build OrderService.sln` exit 0, 0 warnings (first attempt) | HARD | PASS |
| Passes | `dotnet test` exit 0 — 16/16 passed (12 OrderServiceTests incl. Theory rows + 4 OrdersControllerTests) | HARD | PASS |
| Case recall | 13/13 golden branches covered = 1.00 | ≥0.90 | PASS |
| Case precision | 0 EXCLUDE violations | 0 | PASS |
| Forbidden patterns | 0 hits (WebApplicationFactory, TestServer, Testcontainers, JsonSerializer.Serialize, JsonConvert, ServiceCollection/BuildServiceProvider, Arg.Any-in-Received: all 0) | 0 | PASS |
| Conventions | 8/8 applicable checklist items = 1.00 | ≥0.90 | PASS |

**Overall: PASS**

## Recall detail

All 13 golden cases matched: both private `CalculateDiscount` branches driven through `CreateOrder` inputs (inclusive boundary total=100m pinned), validation guards as `[Theory]` rows (empty/whitespace ProductId; 0/−1 Quantity — same branch, one case each), gateway approve/decline/timeout (with `WithInnerException<TimeoutException>`), repository found/not-found, and all four controller status codes (200/404/201/400) via direct instantiation.

Extra generated cases beyond the golden list: `CreateOrder_ValidRequest_SavesMappedOrderAndReturnsSavedOrder` (field-mapping + return-passthrough split from the below-threshold total case) — judged distinct observable behaviors, not EXCLUDE violations (precedent: 2026-07-30 run).

## Misses

None.

## Notes

- `Arg.Any<Order>` appears only inside `DidNotReceive().Save(...)` — the correct idiom for asserting no call happened; judged legitimate (not a capture-evasion hit).
- AAA comments used throughout; exception tests follow the standardized `// Arrange` + `// Act & Assert` convention. `actual`/`expected` prefixes present; DTO capture via `Arg.Do` with field assertions; builders for test data.
- Controller run correctly declined to test `[ApiController]` automatic model validation (pipeline concern, bypassed by direct instantiation).
- New guardrails exercised: Step 0 resolved the target to one file; scoped build/test commands used; no packages added beyond the pre-approved test-project set; the Step 5 scope check correctly flagged unrelated pre-existing working-tree changes without reverting them.
- **Harness inconsistency (for maintainer):** `eval-skills/SKILL.md` names only `OrderService.cs` as the fixture's primary target, but `expected-cases.md` includes 4 `OrdersController` cases. This run covered the controller via a follow-up dispatch (matching the 2026-07-30 precedent target). Either add `OrdersController.cs` to the eval target spec or split the golden list.
- Environment: machine has .NET 9/10 runtimes only; `DOTNET_ROLL_FORWARD=LatestMajor` env var used for `dotnet test` (no project files modified — cleaner than the 2026-07-30 run, which edited the generated test csproj).
- Fixture integrity: nothing under `src/` modified; `OrderService.sln` gained the test project via `dotnet sln add` (additive, required to build); fixture reset to HEAD after scoring.
